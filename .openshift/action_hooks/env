#!/bin/bash

export GOLLUM_PATH="${OPENSHIFT_DATA_DIR}wikidata.git"

echo "env"
echo $DEBUG

if $DEBUG
then
  export

  echo
  echo "debug HOOK_PATH=$(dirname ${BASH_SOURCE}) "
  echo

  export DEBUG=false
fi

